<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "mirror" %></title>
    <link href="https://fonts.googleapis.com/css?family=Quicksand" rel="stylesheet">
    <script src="https://use.fontawesome.com/4d47250886.js"></script>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="custom-bar">
      <div class="title-bar custom-title-bar">
        <div class="title-bar-left inline">
          <span class="title-bar-title">
            <% if current_user %>
              <%= link_to raw("<i class='fa fa-times-circle fa-2x custom-menu-icon' aria-hidden='true'></i>"), destroy_user_session_path, method: :delete  %>
              <span class="current-user-name">signed in as: <%= current_user.username %></span>
            <% end %>
          </span>
        </div>
        <div class="title-bar-right site-logo">
          <%= image_tag('logo.png')%>
        </div>
      </div>
      <% flash.each do |key, value| %>
        <div class="flash-container">
            <%= value %>
        </div>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
