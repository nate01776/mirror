<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "mirror" %></title>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans" rel="stylesheet">
    <script src="https://use.fontawesome.com/4d47250886.js"></script>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="custom-bar">
      <div class="title-bar custom-title-bar">
        <div class="title-bar-left inline">
          <span class="title-bar-title">
            <% if current_user %>
            <div class="small-4 columns">
              <%= link_to 'X', destroy_user_session_path, method: :delete, class:"button new-salon-button", id:'sign-out' %>
            </div>
            <div class="small-6 columns">
              <span class="current-user-name "><p><b>signed in as:</b> <%= current_user.username %></p></span>
            </div>
            <% end %>
          </span>
        </div>
        <div class="title-bar-right site-logo">
          <%= link_to (image_tag('logo.png')), root_path %>
        </div>
      </div>
      <% flash.each do |key, value| %>
        <div class="flash-container">
            <%= value %>
        </div>
      <% end %>
    </div>
    <%= yield %>
    <div class="footer page-footer full-width">
        <h4>site created by nathan wright - 2016</h4>
    </div>
  </body>
</html>
