<div class="row ">
  <div class="small-12 large-8 large-centered medium-centered columns user-box">
    <div class="row">
      <h1>edit <%=@salon.name %></h1>
    </div>
    <div class="row">
      <% if @salon.image.url != nil %>
        <%= image_tag @image, id:"upload-image" %>
      <% end %>
      <%= render 'edit_form' %>
    </div>
  </div>
</div>
